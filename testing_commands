pkg.json :    // "netlify-build": "ROLLUP_SKIP_NODEJS=true nuxt build"


[build]
  command = "npm run netlify-build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  ROLLUP_SKIP_NODEJS = "true" 